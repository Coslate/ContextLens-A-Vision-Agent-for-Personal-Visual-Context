[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "contextlens"
version = "0.1.0"
description = "A vision agent for personal visual context"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["contextlens*"]
